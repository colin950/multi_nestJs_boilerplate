export enum UserStatus {
  LOCKED = 'locked',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum ServiceName {
  COMMON = 0,
  GOOGLE = 1,
}

export enum HttpMethod {
  POST = 'post',
  GET = 'get',
}
