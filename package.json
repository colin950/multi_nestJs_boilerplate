{
  "name": "web-be-boilerplate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "api": "pnpm --filter api run start",
    "bot": "pnpm --filter bots run start",
    "build:api": "pnpm --filter api run build",
    "build:bot": "pnpm --filter bots run build",
    "test:api": "pnpm --filter api run test",
    "install:api": "pnpm install --filter @libs --filter api...",
    "install:bot": "pnpm install --filter @libs --filter bots..."
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.828.0",
    "@nestjs/axios": "^3.1.3",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/mongoose": "^10.1.0",
    "@nestjs/platform-express": "^10.0.0",
    "@nestjs/swagger": "^7.1.13",
    "@nestjs/terminus": "^10.1.1",
    "@nestjs/typeorm": "^10.0.2",
    "@slack/webhook": "^7.0.5",
    "@swc/cli": "^0.5.2",
    "@types/cookie-parser": "^1.4.8",
    "nanoid": "^5.1.5",
    "typeorm": "^0.3.22",
    "typescript": "^5.8.3",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@nestjs/config": "^3.1.1"
  },
  "packageManager": "pnpm@10.11.0"
}
